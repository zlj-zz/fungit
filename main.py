import os
from box import create_boxs, create_git_tree
from pprint import pprint

t = {}
create_git_tree(t)
w = os.get_terminal_size().columns
h = os.get_terminal_size().lines


def main():
    pass


if __name__ == '__main__':
    main()
